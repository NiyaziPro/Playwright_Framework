{
  "name": "playwright-framework",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "chrome": "npx playwright test --headed --project=chromium",
    "firefox": "npx playwright test --headed --project=firefox",
    "webkit": "npx playwright test --headed --project=webkit",
    "smoke": "npx playwright test --headed --project=chromium --grep @smoke",
    "regression": "npx playwright test --headed --project=chromium --grep @regression"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/node": "^20.12.0",
    "allure-playwright": "^3.2.2"
  },
  "dependencies": {
    "@faker-js/faker": "^9.7.0",
    "@playwright/test": "^1.52.0",
    "playwright": "^1.52.0"
  }
}
